package leetcode

object Solution21 {

//  class ListNode(_x: Int = 0, _next: ListNode = null) {
//    var next: ListNode = _next
//       var x: Int = _x
//  }
//
//  def mergeTwoLists(list1: ListNode, list2: ListNode): ListNode = {
//    def toListRecursive(remaining: ListNode, acc: ListNode): Lioom
//    stNode = {
//      if(remaining == null) acc else
//        toListRecursive(remaining = remaining.next, remaining.x :: acc)
//    }
//
//    val (left, right) = (toListRecursive(list1, List()), toListRecursive(list2, List()))
//    left.flatMap(x => x :: right).sortWith((x, y) => x < y)
//  }

}
